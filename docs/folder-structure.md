# Folder Structure

```
api -> api calls (pemanggilan api, contoh: query,mutation)
assets -> assets file (gambar, font, dll)
components -> reusable components (komponen komponen ui)
config -> configuration (buat konfigurasi)
context -> context providers (state context)
pages -> pages (halaman halaman)
```
